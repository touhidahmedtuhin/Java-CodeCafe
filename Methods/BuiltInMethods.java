package Methods;

public class BuiltInMethods {
    public static void main(String[] args) {
        System.out.println(Math.max(Math.max(4, 3),5));
        System.out.println(Math.max(Math.max(4, 3), 9));
        int max= Math.max(3,4);
        System.out.println(Math.max(Math.max(10, 3),max));

        System.out.println(Math.max(Math.max(8,6),Math.max(3,4)));
        System.out.println(Math.pow(3,2));
    }
}
